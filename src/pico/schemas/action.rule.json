{
  "$id": "pico/schemas/action.rule.json",
  "$schema": "http://json-schema.org/draft-07/schema#",

  "type": "object",
  "properties": {
    "act": { "type": "string", "enum": ["rule"] },
    "rule": { "$ref": "rule.json" }
  },
  "required": ["act", "rule"]
}
